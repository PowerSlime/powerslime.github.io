import { LoadingOutlined } from "@ant-design/icons";
import React from "react";

const Loader = () => {
    return (
        <div>
            <LoadingOutlined />
        </div>
    );
};

export default Loader;
